<?php
// PHPDoc is a documentation generator for PHP code. It uses special comments in the 
// code to generate documentation in various formats, such as HTML or XML. 
// These comments start with /** and end with */, and they can contain tags that 
// provide information about the code's purpose, parameters, return values, and more.

// Here's an example of a PHPDoc comment:

/**
 * Adds two numbers and returns the result.
 *
 * @param int $a The first number.
 * @param int $b The second number.
 * @return int The sum of $a and $b.
 */
function add(int $a, int $b): int {
    return $a + $b;
}
