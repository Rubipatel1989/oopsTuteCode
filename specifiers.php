<?php

A type specifier that says what type the argument data should be treated as.

1. %  => a literal percent character. No argument need.
2. b  => The argument treated as integer and present as binary number.
3. c  => the argument treated as integer and presend as the character with ASCII value
4. d  => the argument treated as integer and present as decimal value.